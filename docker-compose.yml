version: '3.7'
services:
  app:
    image: skep/app
    networks:
      - skep
    ports:
      - "8080:8080"
    volumes:
      - "/var/run/docker.sock:/var/run/docker.sock:ro"

  stats:
    image: skep/stats
    volumes:
      - "/proc:/hostfs/proc:ro"
    environment:
      SKEP_HOST_URL: http://app:8080
      LINUX_METRICS_ROOT_FS: '/hostfs'
      DISK_DRIVES: 'sda:sda1'
      NETWORK_INTERFACES: 'eth0'
      LOG_LEVEL: 'DEBUG'

    networks:
      - skep

networks:
  skep: {}
